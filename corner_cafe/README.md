---
title: Corner cafe
sdk: static
app_file: index.html
sdk_version: "1.1"
emoji: ☕
colorFrom: yellow
colorTo: gray
pinned: false
short_description: Corner cafe — static restaurant frontend (v1.1)
---

# Corner cafe

**v1.1** — Opening hours Mon–Sat 9:00–16:00 (Sunday closed). Static frontend (HTML/CSS/JS). Media loads from the public `PIANDT/sushi_atelier_artifacts` dataset.

> Hugging Face free tier no longer allows new Docker Spaces, so this Space uses the **static** SDK. `Dockerfile` / `app.py` remain for local Docker runs.
